# Default environment: NVIDIA GPU training with PyTorch CUDA 12.8.
# Core game and light Q-learning still use only the Python standard library.
--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple
torch==2.11.0+cu128
numpy
PyYAML
